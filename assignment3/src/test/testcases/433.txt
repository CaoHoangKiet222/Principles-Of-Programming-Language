
        x: integer;
        inc: function auto(out n: integer, delta: integer) {
            n = n + delta;
        }
        main: function void() {
            delta: integer = inc(inc(x, x), x);
            y: integer = inc(x, 1) + 1;
        }
        